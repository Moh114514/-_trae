# ============================================================
# BEIMS 建筑能源智能管理系统 — Python 依赖清单
# 版本: v2.0
# 更新: 2026-04-18
# Python 版本要求: >= 3.10
# ============================================================

# 安装方式（任选其一）：
#
# 方式 A：完整安装（包含所有可选功能）
#   pip install -r requirements.txt
#
# 方式 B：最小安装（仅后端 API + 数据库，不含知识库）
#   pip install fastapi "uvicorn[standard]" requests pydantic psycopg2-binary pandas numpy
#
# 方式 C：按需添加
#   pip install chromadb sentence-transformers        # 知识库
#   pip install pdfplumber                            # PDF 支持
#   pip install python-docx                           # Word 支持


# ────────────────────────────────────────────────────────────
# 一、核心依赖（后端 API 运行必需）
# ────────────────────────────────────────────────────────────

# Web 框架（FastAPI + ASGI 服务器）
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP 客户端（调用 Ollama 本地模型 / 阿里云 DashScope API）
requests>=2.31.0

# 数据模型验证（API 请求/响应模型）
pydantic>=2.0.0

# PostgreSQL 数据库驱动
psycopg2-binary>=2.9.0


# ────────────────────────────────────────────────────────────
# 二、数据分析与计算
# ────────────────────────────────────────────────────────────

pandas>=2.0.0
numpy>=1.24.0


# ────────────────────────────────────────────────────────────
# 三、知识库系统（RAG 检索增强生成）
# ────────────────────────────────────────────────────────────

# 向量数据库（文档向量存储与语义检索）
chromadb>=0.4.0

# Embedding 模型（多语言语义编码，约 400MB）
sentence-transformers>=2.2.0


# ────────────────────────────────────────────────────────────
# 四、可选依赖 — 文档格式支持
# ────────────────────────────────────────────────────────────

# PDF 文档解析（知识库读取 .pdf 文件，如国标/行标 PDF）
pdfplumber>=0.10.0

# Word 文档解析（知识库读取 .docx / .doc 文件）
python-docx>=1.0.0


# ────────────────────────────────────────────────────────────
# 五、底层依赖（自动安装，列出供参考）
# ────────────────────────────────────────────────────────────

# PyTorch（CPU 版本，sentence-transformers 的底层依赖）
# Windows CPU 用户建议用官方源，避免下载巨大的 CUDA 版本：
# pip install torch --index-url `https://download.pytorch.org/whl/cpu`
--extra-index-url `https://download.pytorch.org/whl/cpu`
torch>=2.0.0


# ============================================================
# 外部依赖（不在 pip 中安装，需手动准备）
# ============================================================
#
# 1. Ollama + qwen2.5:7b 本地模型（约 4GB）
#    下载安装: `https://ollama.com/download`
#    拉取模型: ollama pull qwen2.5:7b
#
# 2. PostgreSQL 数据库（13+）
#    下载安装: `https://www.postgresql.org/download/`
#    创建数据库: CREATE DATABASE building_energy;
#
# 3. 阿里云 DashScope API Key（云端模型 qwen-plus）
#    申请地址: `https://dashscope.console.aliyun.com/`
#
# 4. Hugging Face Embedding 模型（首次运行自动下载，约 400MB）
#    模型: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
#    国内网络建议设置镜像:
#      环境变量 HF_ENDPOINT = `https://hf-mirror.com`
#
# ============================================================